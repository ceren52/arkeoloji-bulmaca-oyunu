const choices=document.querySelector('#choices'),message=document.querySelector('#message'),next=document.querySelector('#next');let picked=[];
choices.addEventListener('click',e=>{if(e.target.tagName!=='BUTTON')return;picked.push(e.target.dataset.v);e.target.disabled=true;if(picked.length===3){if(picked.join('')==='123'){message.textContent='Doğru! Tapınak kapısında yeni bir işaret belirdi.';next.textContent='Bulmaca 2 hazır: yakında yeni semboller eklenecek.';document.querySelector('.locked').style.opacity=1}else{message.textContent='Sıra yanlış. Yenile ve tekrar dene.'}}});
